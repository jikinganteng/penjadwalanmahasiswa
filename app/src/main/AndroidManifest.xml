<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PenjadwalanMahasiswa"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        tools:targetApi="31">

        <!-- Splash screen sebagai activity pertama saat aplikasi dijalankan -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity utama setelah splash -->
        <activity
            android:name=".HomeActivity"
            android:exported="true" />

        <!-- Activity untuk daftar kegiatan -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- Activity untuk menambah kegiatan -->
        <activity
            android:name=".TambahKegiatanActivity"
            android:exported="true" />

        <!-- Activity untuk melihat detail kegiatan -->
        <activity
            android:name=".DetailKegiatanActivity"
            android:exported="true" />

        <!-- Activity tentang aplikasi -->
        <activity
            android:name=".TentangActivity"
            android:exported="true" />
    </application>

</manifest>
